<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mongodb2</title>
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-800 text-white p-10">
      <div class="nav mb-10">
        <a href="/read" class="text-blue-500">All users</a>
      </div>
        <h1 class="text-3xl mb-3">Update User</h1>
      <form action="/update/<%= user.id %>" method="post">
        <input
          type="text"
          class="px-5 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md"
          placeholder="name enter here"
          name="name"
          value="<%= user.name %>"
        />
        <input
          type="text"
          class="px-5 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md"
          placeholder="name mail here"
          name="email"
          value="<%= user.email %>"
        />
        <input
          type="text"
          class="px-5 py-2 outline-none bg-transparent border-2 border-zinc-600 rounded-md"
          placeholder="name image url here"
          name="image"
          value="<%= user.image %>"
        />
        <input type="submit" value="Update user" class="px-3 py-2 bg-yellow-500 text-black text-lg rounded-lg ml-3 cursor-pointer">
      </form>
    </div>
  </body>
</html>
