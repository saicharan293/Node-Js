<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mongodb2</title>
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 text-white p-10">
      <div class="nav mb-2">
        <a href="/" class="text-blue-500">Back to home</a>
      </div>
      <h1 class="text-3xl mb-3">All Users</h1>
      <div class="users flex gap-4 flex-wrap">
        <% if (users.length > 0) { %> <% users.forEach(function(user){ %>
        <div class="user h-64 w-60 p-4 bg-zinc-700 rounded-lg">
          <div class="w-full h-36 overflow-hidden bg-zinc-500">
            <img
              class="w-full h-full object-cover"
              src="<%= user.image %>"
              alt=""
            />
          </div>
          <h3 class="text-2xl tracking-tighter"><%= user.name %></h3>
          <h3 class="text-zinc-500"><%= user.email %></h3>
          <div class="flex gap-3 mt-2">
            <a href="" class="text-zinc-500">Edit user</a>
            <a href="/delete/<%= user.id %>" class="text-red-500"
              >Delete user</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-500">No users added yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
