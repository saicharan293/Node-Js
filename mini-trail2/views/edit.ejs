<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile page</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end gap-3">

            <a href="/logout" class="bg-red-500 px-3 py-2 text-sm rounded-md mb-2 inline-block">Logout</a>
            <a href="/profile" class="bg-blue-500 px-3 py-2 text-sm rounded-md mb-2 inline-block">Profile</a>
        </div>
      <h4 class="mb-3">Edit your post</h4>
      <form method="post" action="/update/<%= post._id %>">

          <textarea
          placeholder="what's on your mind"
          class="w-1/3 p-2 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md block"
          name="content"
          id=""
          ><%= post.content %></textarea>
          <input
          type="submit"
          value="Update post"
          class="px-3 text-md py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40 cursor-pointer"
          />
        </form>
        
    </div>
  </body>
</html>
